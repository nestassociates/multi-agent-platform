# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Apex27 Main API (Standard API - primary data source)
# Provides full data access, webhook support, and structured flag objects
# Supports real-time webhooks + periodic full sync
APEX27_API_KEY=your-main-api-key

# Apex27 Portal API (optional - for GDPR-compliant public queries)
# Limited to marketed properties only, no webhooks
# Use if you need public-facing property search separate from Main API
APEX27_PORTAL_URL=https://portals-xxxxx.apex27.co.uk
APEX27_PORTAL_API_KEY=your-portal-api-key

# Cron Security (protect cron endpoints)
CRON_SECRET=generate-random-32-char-secret

# OS Data Hub API (UK Property Data)
OS_DATA_HUB_API_KEY=your-api-key
OS_DATA_HUB_API_SECRET=your-api-secret

# Mapbox (for territory mapping)
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=your-mapbox-token

# Vercel (for build system)
VERCEL_API_TOKEN=your-vercel-token
VERCEL_TEAM_ID=your-team-id

# Email (Resend)
RESEND_API_KEY=your-resend-key
RESEND_FROM_EMAIL=noreply@nestassociates.com

# Sentry (optional for local dev)
NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn
SENTRY_AUTH_TOKEN=your-sentry-token

# Upstash Redis (distributed rate limiting)
# Creates persistent, serverless-native rate limiting that works across
# all Vercel serverless functions and survives redeployments.
#
# Setup:
# 1. Create free account at https://console.upstash.com
# 2. Create new Redis database (select region closest to your Vercel deployment)
# 3. Copy REST URL and REST Token from database details page
#
# Rate limits configured:
# - Login: 5 attempts per email per 15 minutes (prevents brute force)
# - Contact form: 5 submissions per IP per hour (prevents spam)
#
# If not configured, falls back to in-memory rate limiting (resets on each deployment)
UPSTASH_REDIS_REST_URL=https://your-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token

# Google Analytics 4 (for agent analytics dashboard)
# Setup:
# 1. Create GA4 property at https://analytics.google.com
# 2. Enable Google Analytics Data API in Google Cloud Console
# 3. Create service account with GA4 Viewer role
# 4. Add service account email as Viewer in GA4 property
# 5. Copy credentials below
#
# If not configured, dashboard shows mock data for development
GA4_PROPERTY_ID=123456789
GA4_SERVICE_ACCOUNT_EMAIL=ga4-reader@project.iam.gserviceaccount.com
GA4_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Environment
NODE_ENV=development
